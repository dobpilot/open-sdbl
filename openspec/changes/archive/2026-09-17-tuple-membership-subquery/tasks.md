## 1. Implementation

- [x] 1.1 Parse the tuple and compile the membership test as `EXISTS`.

## 2. Verification and documentation

- [x] 2.1 Goldens on both dialects, in `ГДЕ` and in a virtual-table
  condition; the UNF corpus rerecorded; the language table; the five CI
  checks and strict OpenSpec validation.
