## 1. Workflow

- [x] 1.1 Add a job that archives the tracked sources of the commit and
  uploads the archive.
- [x] 1.2 Add a job that builds the CLI for Windows MSVC in release and
  uploads the executable.

## 2. Verification

- [x] 2.1 Check the workflow file parses and the jobs run green on a push.
