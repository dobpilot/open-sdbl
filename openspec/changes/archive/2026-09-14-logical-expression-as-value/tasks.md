## 1. Implementation

- [x] 1.1 Render the logical expressions from one place, plain in a
  predicate position and in the dialect's boolean value form in a value
  position.

## 2. Verification and documentation

- [x] 2.1 Platform probes for `ПОДОБНО` as a column and for a logical
  expression with a `NULL` operand; goldens for both dialects; re-record
  the demo corpus; update `docs/query-language-support.md`; run the five
  CI checks and strict OpenSpec validation.
