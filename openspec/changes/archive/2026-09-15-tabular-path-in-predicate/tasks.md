## 1. Compilation

- [x] 1.1 Recognize `<источник>.<Состав>.<Поле>` in a comparison and
  compile it into a correlated `EXISTS` over the section.
- [x] 1.2 Keep the refusal everywhere else, with a diagnostic that says
  what the path means.

## 2. Verification

- [x] 2.1 Compare the probe answers with the platform, including the
  owner that has two matching rows.
- [x] 2.2 Re-record the corpus, execute on the live base, and update the
  documentation.
- [x] 2.3 Run formatting, Clippy with warnings denied, workspace tests,
  rustdoc with warnings denied, the bounded fuzz checks, and strict
  OpenSpec validation.
