## 1. Implementation

- [x] 1.1 Decode `.9` rows with the generalized row shape and tag the
  source; keep `.9` for charts of accounts at resolution.
- [x] 1.2 Fetch `.9` in both providers' acquisition queries and in the
  corpus tool; reload the UNF fixture.
- [x] 1.3 Admit charts of accounts to `ЗНАЧЕНИЕ`.

## 2. Verification and documentation

- [x] 2.1 Unit test on the measured `.9` rows; a UNF test on
  `ЗНАЧЕНИЕ(ПланСчетов.Управленческий.ПрочиеРасходы)`; the corpus
  rerecorded; the language table; the five CI checks and strict
  OpenSpec validation.
