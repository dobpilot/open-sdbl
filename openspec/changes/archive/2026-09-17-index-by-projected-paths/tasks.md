## 1. Implementation

- [x] 1.1 `IndexField` keeps the segments; the check matches labels,
  requested names and projected field paths.

## 2. Verification

- [x] 2.1 Test in `query_compile`; corpora rerecorded; the five CI
  checks.
