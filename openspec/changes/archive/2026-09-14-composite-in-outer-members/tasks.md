## 1. Implementation

- [x] 1.1 Read the members of a composite value on the outer side of
  `В (…)` and compare the string member as text.

## 2. Verification and documentation

- [x] 2.1 Run the platform probes through the compiler against the same
  base until they agree; regression goldens for both faults; run the five
  CI checks and strict OpenSpec validation.
