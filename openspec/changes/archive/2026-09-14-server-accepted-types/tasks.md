## 1. Implementation

- [x] 1.1 State the type of a null value where the server resolves by it.
- [x] 1.2 Report the string kind of a concatenation.
- [x] 1.3 Reconcile stored and derived string types in alternatives,
  comparisons and joins.
- [x] 1.4 Join a composite hop from its own alias.

## 2. Verification and documentation

- [x] 2.1 Execute every compiled corpus query against a real base until the
  server accepts all of them; keep the platform probe comparison green;
  goldens; re-record the demo corpus; update
  `docs/query-language-support.md`; run the five CI checks and strict
  OpenSpec validation.
