## 1. Implementation

- [x] 1.1 The bucketed relation with running sums per grain; the
  variant chosen by the split fields and balances the statement reads.
- [x] 1.2 The accumulation and the accounting table on top of it; the
  SQL Server 2008 refusal.

## 2. Verification and documentation

- [x] 2.1 Tests on both fixtures and both dialect levels; the SQL
  executed on the live UNF base; both corpora rerecorded; the language
  table; the five CI checks and strict OpenSpec validation.
