## 1. Implementation

- [x] 1.1 `ParsedConfigResource.roles` from the roles collection.
- [x] 1.2 `metadata::roles`: `Right`, `RoleRights`, `parse_role_rights`,
  `RoleCatalog`.
- [x] 1.3 `role_rights` acquisition statements for both providers.

## 2. Verification

- [x] 2.1 Tests on the БП 3.0 and УНФ fixtures under
  `tests/fixtures/access`; the five CI checks.
