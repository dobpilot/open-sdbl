## 1. Implementation

- [x] 1.1 `parse_index_fields` keeps the last segment of a dotted field.

## 2. Verification

- [x] 2.1 Test in `query_compile`; corpora rerecorded; the language
  table; the five CI checks.
