## 1. Implementation

- [x] 1.1 Return the reference joins of a compiled condition and render
  them after the `FROM` of every relation that takes a condition.

## 2. Verification and documentation

- [x] 2.1 Tests on the UNF fixture for both register kinds; the SQL
  executed on the live UNF base; the corpus rerecorded; the language
  table; the five CI checks and strict OpenSpec validation.
