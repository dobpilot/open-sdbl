## 1. Implementation

- [x] 1.1 The store remembers the roles whose rights are absent.
- [x] 1.2 `\as` reports and skips them; `\role` and `\template` report.

## 2. Verification

- [x] 2.1 Tests; the five CI checks.
