## 1. Implementation

- [x] 1.1 Record how many columns each projected value occupies so a union
  can line its branches up by value.
- [x] 1.2 Spread a value over the members of a composite in every branch
  that does not already project them.

## 2. Verification and documentation

- [x] 2.1 Platform probes for every union shape; goldens; re-record the
  demo corpus; update `docs/query-language-support.md`; run the five CI
  checks and strict OpenSpec validation.
