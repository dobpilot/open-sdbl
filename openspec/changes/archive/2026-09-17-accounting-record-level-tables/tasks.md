## 1. Implementation

- [x] 1.1 `ОборотыДтКт` over the unfolded record with both sides' extra
  dimensions, positional and listed.
- [x] 1.2 `ДвиженияССубконто` as the filtered records with the extra
  dimensions renamed.

## 2. Verification and documentation

- [x] 2.1 Tests on the buh and unf fixtures; the SQL executed on the
  live buh base; the corpora rerecorded; the language table; the five
  CI checks and strict OpenSpec validation.
