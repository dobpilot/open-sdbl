## 1. Compilation

- [x] 1.1 Compile `ПРЕДСТАВЛЕНИЕ` of a `ВЫБОР` by presenting each branch
  and joining them into one string column.
- [x] 1.2 Refuse a branch that would need the deferred protocol, naming
  the reason.

## 2. Verification

- [x] 2.1 Compare the probe answers with the platform.
- [x] 2.2 Re-record the corpus, execute on the live base, and update the
  documentation.
- [x] 2.3 Run formatting, Clippy with warnings denied, workspace tests,
  rustdoc with warnings denied, the bounded fuzz checks, and strict
  OpenSpec validation.
