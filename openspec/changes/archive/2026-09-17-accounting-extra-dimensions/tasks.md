## 1. Implementation

- [x] 1.1 `.7` predefined items, the `AccRgED` kind, the `Субконто`
  service table, `ЗНАЧЕНИЕ` over a chart of characteristic types.
- [x] 1.2 `Субконто<k>`/`ВидСубконто<k>` in the fold, positional and by
  the listed kinds; the account filter of the explicit list.

## 2. Verification and documentation

- [x] 2.1 Tests on the buh fixture; the SQL executed on the live buh
  base; the corpora rerecorded; the language table; the five CI checks
  and strict OpenSpec validation.
