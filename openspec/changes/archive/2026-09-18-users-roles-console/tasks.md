## 1. Implementation

- [x] 1.1 `attach_roles`/`roles`, `RoleCatalog::from_snapshot`,
  `object_query_name`.
- [x] 1.2 The pipeline collects the roles and keeps the layout.
- [x] 1.3 `crates/open-sdbl-cli/src/access.rs`: the store, the commands,
  the user-derived restrictions.
- [x] 1.4 The console loop, help, README; version 0.5.5.

## 2. Verification

- [x] 2.1 Unit tests of the commands and the derivation on the fixtures;
  a live check on the УНФ base; the five CI checks.
