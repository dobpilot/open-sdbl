## 1. Implementation

- [x] 1.1 Compare a composite subquery member by member, spreading the
  other side over the same members.

## 2. Verification and documentation

- [x] 2.1 Platform probes for reference/string/composite against a
  composite subquery; a golden for both dialects; re-record the demo
  corpus; update `docs/query-language-support.md`; run the five CI checks
  and strict OpenSpec validation.
