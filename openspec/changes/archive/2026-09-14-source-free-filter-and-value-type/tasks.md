## 1. Implementation

- [x] 1.1 Render `ГДЕ` of a source-free statement as a `WHERE` without a
  `FROM`.
- [x] 1.2 Name the `Type` column of a chart of characteristic types.

## 2. Verification and documentation

- [x] 2.1 Platform probes for both, including a true and a false
  condition.
- [x] 2.2 Goldens; re-record the demo corpus; update
  `docs/query-language-support.md`; run the five CI checks and strict
  OpenSpec validation.
