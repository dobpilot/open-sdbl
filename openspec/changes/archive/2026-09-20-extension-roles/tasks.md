## 1. Implementation

- [x] 1.1 `parse_serialized_sequence` for a resource of several records.
- [x] 1.2 `ContentKey`, `extension_root_key`, `parse_extension_index`.
- [x] 1.3 The statements of `_ExtensionsInfo` and of one stored resource.
- [x] 1.4 `RoleCatalog::extend`, and the console reading extension roles.

## 2. Verification

- [x] 2.1 Tests on fixtures of the demo base; a live check on MSSQL; the
      five CI checks.
