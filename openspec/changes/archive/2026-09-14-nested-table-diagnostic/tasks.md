## 1. Implementation

- [x] 1.1 Report the nested tabular-section projection by name when a
  field path is followed by `.(…)` or `.*`.

## 2. Verification and documentation

- [x] 2.1 Goldens for both spellings; re-record the demo corpus; update
  `docs/query-language-support.md`; run the five CI checks and strict
  OpenSpec validation.
