## 1. Implementation

- [x] 1.1 The periodicity maps to a windowed grain; the recorder and the
  line number join the split fields.

## 2. Verification

- [x] 2.1 Tests on the demo and buh fixtures; the SQL executed on the
  live buh base and checked for opening plus turnover equals closing;
  corpora rerecorded; the language table; the five CI checks.
