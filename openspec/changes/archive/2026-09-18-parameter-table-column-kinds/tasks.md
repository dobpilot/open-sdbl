## 1. Implementation

- [x] 1.1 `ParameterColumn` with a `ColumnKind`; validation by kind;
  casts in the first row; typed empty table.
- [x] 1.2 Console literal `Имя КАК Вид`; corpus tag with kinds and the
  inference in `bind_tables.py`.

## 2. Verification

- [x] 2.1 Tests updated; the SQL executed on the live base; corpora
  rerecorded; README and the language table; the five CI checks.
