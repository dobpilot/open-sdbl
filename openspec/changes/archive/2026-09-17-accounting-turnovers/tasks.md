## 1. Implementation

- [x] 1.1 Fold the record into its two sides and aggregate by account,
  dimensions and period; the three turnover columns per resource.
- [x] 1.2 The account condition and the condition per side; the staged
  diagnostics for what the stage leaves out.

## 2. Verification and documentation

- [x] 2.1 Tests on the UNF fixture; the generated SQL executed on the
  live UNF base; the corpus rerecorded; the language table; the five CI
  checks and strict OpenSpec validation.
