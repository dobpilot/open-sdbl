## 1. Implementation

- [x] 1.1 `КорСчет` and `<Измерение>Кор` as folded fields with swapped
  sides.
- [x] 1.2 `КорСубконто<k>` positional and listed, the presence predicate
  on the opposite side of each branch.
- [x] 1.3 `УсловиеКорСчета` compiled with the account condition and the
  condition over the side's fields.

## 2. Verification

- [x] 2.1 Tests on buh and unf; the SQL executed on the live buh base and
  reconciled with the record total; corpora rerecorded; the language
  table; the five CI checks.
