## 1. Implementation

- [x] 1.1 `StoredValueRef`, `parse_stored_value_ref`, `decode_stored_value`
      and the map readers.
- [x] 1.2 The `binarydata` statement of both providers.
- [x] 1.3 `\as` reads the register and fills the parameters.

## 2. Verification

- [x] 2.1 Tests; a live check on the УНФ base; the five CI checks.
