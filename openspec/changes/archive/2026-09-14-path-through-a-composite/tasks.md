## 1. Implementation

- [x] 1.1 Walk the rest of a reference path inside every target of a
  composite hop, nesting again when a deeper hop is composite too.

## 2. Verification and documentation

- [x] 2.1 Platform probes for composite→fixed→field, composite→composite
  →field and a recorder pair; goldens; re-record the demo corpus; update
  `docs/query-language-support.md`; run the five CI checks and strict
  OpenSpec validation.
