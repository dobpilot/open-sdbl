## 1. Implementation

- [x] 1.1 `record_limit` and `record_order`; the relation takes the
  prefix, the ordering and the limit of the dialect.

## 2. Verification

- [x] 2.1 Tests on the buh fixture; corpora rerecorded; the language
  table; the five CI checks.
