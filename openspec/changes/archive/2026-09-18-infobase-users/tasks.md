## 1. Implementation

- [x] 1.1 `metadata::users`: `decode_user_data`, `UserData`, `InfoBaseUser`.
- [x] 1.2 `USERS` statements for both providers.

## 2. Verification

- [x] 2.1 Tests on the УНФ demo users under `tests/fixtures/access`; the
  five CI checks.
