## 1. Implementation

- [x] 1.1 `##` as one `#`.
- [x] 1.2 `#ИмяТекущейТаблицы` as a quoted string.
- [x] 1.3 `RestrictionTemplate::new`.

## 2. Verification

- [x] 2.1 Tests of every directive of the manual, of the manual's
      examples, and of the compilation of an expanded template into SQL;
      the five CI checks.
