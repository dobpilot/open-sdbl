## 1. Compilation

- [x] 1.1 Count a presentation of an aggregate as an aggregated
  projection.

## 2. Verification

- [x] 2.1 Compare the probe answers with the platform for a reference
  aggregate, a count, a grouped aggregate and one wrapped in ЕСТЬNULL.
- [x] 2.2 Re-record the corpus, execute on the live base, and update the
  documentation.
- [x] 2.3 Run formatting, Clippy with warnings denied, workspace tests,
  rustdoc with warnings denied, the bounded fuzz checks, and strict
  OpenSpec validation.
