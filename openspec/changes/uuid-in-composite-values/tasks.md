## 1. Codec

- [x] 1.1 Map a unique identifier to the binary member `_B` with the `Null`
  tag, including its zero for other branches and its member kind.

## 2. Verification

- [x] 2.1 Compare the probe answer with the platform for an identifier
  beside a reference, and for `ТИПЗНАЧЕНИЯ` of that value.
- [x] 2.2 Re-record the corpus, execute on the live base, and update the
  documentation.
- [x] 2.3 Run formatting, Clippy with warnings denied, workspace tests,
  rustdoc with warnings denied, the bounded fuzz checks, and strict
  OpenSpec validation.
